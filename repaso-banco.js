let misOperacionesBancarias = [300, 550. -500, 300, 200, -700];

función calcularSaldos (operacionesBancarias){
let saldoDePositos = 0;
let saldoDeRetiros = 0;
let saldoAcuenta = 0;
for (let i= 0; i < operacionesBancarias.length; i++){
    if (operacionesBancaria[i]>0){
        saldoDePositos += operacionesBancarias[i];{
            else{
                saldoDeRetiros -= operacionesBancarias[i]
            }
            saldoAcuenta += operacionesBancarias[i];
        }
        return{
            saldoDePositos,
            saldoDeRetiros,
            saldoAcuenta,
        };
    }

}
}

let nombreDeCliente = "María";
let apellidosDeCliente = "Martinez Allende";
let resultadosDeCuenta = calcularSaldos(misOperacionesBancarias);

function imprimirMensaje(nombre, apellido, callback, operacionesBancarias){
    let resultado = callback
}