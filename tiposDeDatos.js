// Dato del tipo number

let edad = 33;
console.log(edad);

// Dato del tipo number con decimales
let precio = 150.50;
console.log (precio);

// Dato del tipo string con comillas dobles
let nombre = "Miguel";
console.log(nombre);
 
// Dato del tipo string con comillas simples (no sé como se escriben, ayuda)
let apellido = "Martínez"
console.log (apellido)

// Dato tipo booleano con valor true
let verdadero = true;
console.log (verdadero);

// Dato tipo booleano con valor false
let falso = false;
console.log (falso);

// Dato del tipo  NaN 
let notANumber = NaN;
console.log (notANumber);

//Dato del tipo null
let nulo = null;
console.log (nulo);

// Dato undefined
let sinValor;
console.log(sinValor);
// Dato del tipo objeto literal 
let persona = { nombre: "Job",
edad: 28,
vive: "CDMX"
}
console.log(persona);

// Dato del tipo Array
let paises = ["México", "Argentina", "Colombia", "Peru"];
console.log(paises);
